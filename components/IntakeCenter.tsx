'use client';import{useState}from'react';import{FileUp,ArrowRight,Sparkles,CheckCircle2}from'lucide-react';import{createIntake,type IntakeResult}from'../lib/api';
export function IntakeCenter(){const[file,setFile]=useState<File|null>(null),[text,setText]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[result,setResult]=useState<IntakeResult|null>(null);
async function submit(){setBusy(true);setError('');try{setResult(await createIntake({name:file?.name||'New Customer Project',text,file:file||undefined}))}catch(e:any){setError(e?.message||'Backend unavailable')}finally{setBusy(false)}}return <><div className="pageHead"><div><div className="crumb">Home <span>›</span> Intake Center</div><h1>Start a New Engagement</h1><p>Upload an RFI, RFP, SOW, proposal, notes or emails. We&apos;ll extract intelligence and create your engagement.</p></div></div><div className="intakeGrid"><div className="panel uploadPanel"><label className="dropzone"><input type="file" accept=".pdf,.doc,.docx,.xlsx,.xls,.txt,.ppt,.pptx,.eml,.msg" onChange={e=>setFile(e.target.files?.[0]||null)}/><FileUp/><strong>{file?file.name:'Drag & drop files here or click to browse'}</strong><span>Supports PDF, DOCX, XLSX, TXT, PPTX, EML</span><button type="button" className="secondary">Upload Files</button></label></div><div className="panel"><h3>Or Paste / Type Your Content</h3><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Paste RFI, requirements, notes or any other information here..."/><button className="primary continue" onClick={submit} disabled={busy||(!file&&!text)}>{busy?'Processing…':'Continue'} <ArrowRight/></button></div></div><div className="panel recent"><h3>Intake processing</h3>{error?<div className="result" style={{borderColor:'rgba(255,170,80,.35)'}}><Sparkles/><div><strong>Backend connection required</strong><p>{error}</p><span>Start the Python API and set NEXT_PUBLIC_API_BASE_URL if it is not running on localhost:8000.</span></div></div>:result?<div className="result"><CheckCircle2/><div><strong>{result.name}</strong><p>{result.status} · Engagement {result.engagement_id}</p><span>{result.extracted_summary}</span></div></div>:<div className="empty"><Sparkles/>Upload or paste an intake to run the real Python intake engine.</div>}</div></>}
